#include "parser.h"
#include "utils.h"

namespace tools
{
	void Parser::parse(const std::string& httpRequest)
	{
		//
	}

	std::vector<std::string> Parser::tokenize(const std::string& input, const char delim)
	{
		std::vector<std::string> tokens;
		std::string token;

		// Parse string directly by string delim not char

		return tokens;
	}
} // namespace P